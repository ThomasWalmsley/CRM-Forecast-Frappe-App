[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "crm_forecast_project_toml"
version = "0.1.0"
description = "Monthly sales forecast view for Frappe CRM"
requires-python = ">=3.10"
dependencies = []

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
